# a product
type Product {
  # id of the product
  id: ID!
  # name of the product
  name: String!
}

type Category {
  id: ID!
  name: String!
  description: String!
  products: [Product]!
}

type Query {
  allProducts: [Product]!
  allCategories: [Category]!
}
